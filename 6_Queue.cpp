#include <iostream>
#include <queue>

using namespace std;

int main(){
    queue<string> q;

    q.push("devansh");
    q.push("neelansh");
    q.push("hussain");
    q.pop();
    q.empty();
    q.size();
    q.front();
    
}